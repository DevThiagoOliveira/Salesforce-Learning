Opportunity oportunidade = new Opportunity();
Date data = Date.newInstance(2023, 05, 27);

oportunidade.Name = 'Executado pelo visual studeo code';
oportunidade.CloseDate = data;
oportunidade.StageName = 'Prospecting';

insert oportunidade;
